syntax = "proto3";

package acesso;

service AcessoService {
  rpc ValidarToken (Token) returns (Usuario) {}
}

message Token {
  int32 token = 1;
}

message Usuario {
  int32 id = 1;
  string nome = 2;
}